package tde.ui.old.model;

import tefkat.config.TefkatConfig.Model;

public class TargetInstance extends ModelInstance {
    public TargetInstance(TransformTask task, Model model) {
        super(task, model);
    }
}
