package tde.ui.old.model;

import tefkat.config.TefkatConfig.Model;

public class TraceInstance extends ModelInstance {
    public TraceInstance(TransformTask task, Model model) {
        super(task, model);
    }
}
